{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
<div class="card bg-light p-3">
    <h1 class="display-3">Welcome</h1>
    <p class="lead">Symfony {{ constant('Symfony\\Component\\HttpKernel\\Kernel::VERSION') }} &amp; Bootstrap 5.0</p>
    </div>
    <div class="card mb-2 mt-2 bg-info">
        <div class="card-body">
           top row
        </div>
    </div>
    <div class="row">
        <div class="col">
            <div class="card">
                <div class="card-body">
                {% for i in 1..8 %}
                    <div class="{{ cycle(['even', 'odd'], i) }}">
                        <p>Test {{i}}</p>
                    </div>
                {% endfor %}
            </div>   
        </div>
    </div>
    <div class="col">
            <div class="card">
                <div class="card-body">
                    &nbsp;
                </div>
            </div>
        </div>
    <div class="col">&nbsp;</div>
</div>
{% endblock %}
